# The tool call: what owns it, what it owns, and what happens when it ends

> The lifetime model of one sandboxed tool call — binding, call and run — what each owns, and what is reclaimed when the call ends. It is universal to every kind, not a CodeAct feature. Its source of record is [`research/call-lifetime.md`](research/call-lifetime.md); its siblings are [`architecture.md`](architecture.md), the structure a call lives in, and [`hosts.md`](hosts.md), the outward direction where a guest calls host tools.

Several problems turn out to be one: how a kind cleans up after itself, which layer knows how to delete, what happens when two calls share a sandbox, and who decides where a workload's files go. None can be answered without first answering what a tool call is, what it owns, and how long anything lives. In one sentence:

> **A sandbox lives for the conversation, what a call writes should live for the call, and nothing bridges the two.**

`acquire` is get-or-create, so anything a kind writes survives the whole conversation and is readable by every later call in the same sandbox.

## Four lifetimes

```
binding    one per tool               process           SandboxToolBinding
  └ call   one per tool call          the call          ← owns its own guest path
      └ run   0..1, transport only    inside a call     GuestRunLayout, reclaim_run

sandbox    one per (scope, thread_id, agent_dir, kind)   conversation
             ...and per call_id too at IsolationScope.CALL   the call
```

The nesting is real; **the sandbox is not its root.** It sits beside the chain. A binding is built once, before any key exists, and reads scope and thread per call — so one binding reaches as many sandboxes as it serves conversations; and a workload shipping two tools builds two bindings against one sandbox. Neither is "per" the other. They get confused because they coincide in the single-tool, single-conversation case every test exercises.

Four lines is the terse form; the bars below put the same four lifetimes on one time axis, where the coincidence that confuses them comes apart.

![Four lifetimes drawn as bars on one left-to-right time axis. The binding is the topmost bar and spans the whole axis — one per tool, built once before any key exists, holding host configuration only and reading scope and thread per call — while two teal sandbox bars with different spans sit beside it rather than under it, one per conversation, keyed by scope, thread, agent dir and kind. Inside the first conversation's span three amber call bars run in sequence over the one warm sandbox, each ending in a tick, the finally that removes the guest path that call owns; exactly one of them carries a thinner, sharp-cornered run bar — 0..1 per call, host-tools transport only — and the other two carry none. The second conversation carries a call of its own, because one binding reaches as many sandboxes as it serves conversations. Both sandbox bars outlive every call in them and end only with the conversation, where disposal is best-effort and purge asks every registered backend.](assets/four-lifetimes.svg)

## The words

**`call`** — one execution of the tool function. The framework's unit, and the boundary a `finally` sits on. Anything else is qualified: `transport call`, `backend call`, `control-plane call`. **`run`** — the transport's unit: one supervised guest program with a `GuestRunLayout`. Published API, so it is not moving; codeact states the relationship exactly, *"a fresh run per call."* Not **`invocation`**, which is already spent on an external `docker` or `wslc` subprocess.

The concept with no word was the framework's own place in the guest, and both kinds invented one — codeact's `run_dir`, used even where no transport run exists, and bicep's `round_dir`. It is **`guest_call_path()`**, and each word carries something:

- **`call`**, not `run` — bicep runs several guest commands in one place and has no run at all. "One per call" holds for both kinds; "one per run" describes neither.
- **`guest`** — a bare name reads as a path on the machine the agent runs on, the one place it is not. This repository already spends `guest` on that distinction: `guest_path_relative_to`, `guest_run_layout`, `confine_resolve_guest_path`.
- **`path`**, not `directory` — a path is all the protocol promises. `Sandbox.remove`'s contract is *"`path`, and everything under it"*, and `FILES_LIST` is a separate capability precisely because enumeration is not universal. A backend serving its store from memory addresses a place the same way.

Not `guest_call_prefix`: a prefix invites `f"{prefix}name"` with no separator.

| Term | Layer | Visible as |
| --- | --- | --- |
| `run` | `_host_tools_over_exec.py`, stdlib-only core | public: `GuestRunLayout`, `reclaim_run` |
| `binding`, `call` | `maf.py`, the MAF glue | by name only: `from maf_sandbox.maf import ...` |
| — | `_protocol.py` | **nothing** |

`maf.py` is the one module that may import `agent_framework`, kept out of `__init__` by two tests. So `call` cannot become a protocol term without breaking the layering — and the protocol holding none of this vocabulary is not an oversight. There is no protocol for kinds, and nothing here invents one.

## What the binding holds, and what the call owns

Every field the binding carries is host configuration: the router, the caller context, the agent directory, the spec, the name, the logger, the sink. Not one is derived from a caller. The context is the tell — it takes *callables* rather than a scope and a thread, so nothing caller-shaped is ever stored. Generalised past the case it was built for:

> **The binding holds host configuration only. Anything derived from a caller — a scope, a thread, a path generated for one call — lives on the call, or nowhere.**

The distinction is finer than per-tool versus per-call. The binding **already gives per-call answers**: the key, the file listing and the acquired sandbox differ every call. What it never does is *store* one — each is **read** from the host's context when asked. `guest_call_path()` is the first value that must be generated and then remembered, which is why it needs a home of its own.

The binding is shared by every concurrent call, so per-call state cannot be an instance attribute. Put the path there and two parallel calls receive the same one: the second writes its program over the first's, finishes first, and reclaims a place the first is still executing in — a cleanup contract that introduces a worse leak than the one it closes. A `ContextVar` bound by the wrapper is the only object whose lifetime is the call. It carries four fields rather than one path, each answering a way the record was wrong about *which* sandbox owns a call's files: `owner`, because one `ContextVar` serves every binding in the process; `name`, the only field a kind asked for; `acquired` as a **mapping**, because `acquire` takes a key and one call can reach two sandboxes; and `closed`, because a task the body left running still holds the record after the removal has run. It is correct under asyncio — each task starts from a copy of its parent's context, so a body that spawns children has them *read* the right record and nothing a child sets reaches a sibling.

- **It is mutable, and the only mutable thing in the chain**, because its lifetime is one call. The binding is immutable because its lifetime is the process. One rule, two objects.
- **It is private.** A kind calls `binding.guest_call_path()`; the record answers.
- **Asking outside a call raises**, and so does asking after one returned. There is no call, or there is no longer one: returning a path nothing will reclaim is the leak wearing the API meant to close it. It raises rather than returning a message, unlike the binding's three accessors, because reaching it wrongly is a wiring mistake in a kind, not something a model can cause or should be told about.

Both alternatives are closed rather than unattractive. Passing a per-call object into the body would put it in the tool's JSON schema, because the body's signature *is* the schema. Rebuilding the binding per call contradicts the guarantee that `build` runs once, when a tool is attached.

## Cleanup, as a consequence

With the call owning a path, cleanup is the framework's: a `finally` runs it after a result, a refusal and an exception alike, so a kind cannot forget a path it never held. A body that never asked for a path costs nothing, and a synchronous body is not held to the rule at all — it cannot `await acquire`, so it holds no sandbox and owns nothing. A spec whose `work_dir` is the guest root is refused for an async body, because a path one component from the root is one the removal will not perform.

**Disposal is the default; reuse is an explicit host decision.** Directory reclamation, process groups and snapshots cannot prove complete cleanup of a sandbox. `SandboxRouter` therefore defaults to `min_cleanup=Cleanup.DISPOSE`. A host choosing `Cleanup.RECLAIM` accepts residual filesystem and process state, including for kinds that execute arbitrary code. Reuse still attempts process cleanup and reclaims the call directory.

```python
class Cleanup(StrEnum):
    RECLAIM = "reclaim"   # remove the call's directory; the rest of the sandbox stays as the call left it
    RESET   = "reset"     # restore the sandbox to the state it had before any input reached it
    DISPOSE = "dispose"   # delete the sandbox; the next call creates one
```

**Which operations are available.** `DISPOSE` is available on every backend. `RESET` requires `Capability.SNAPSHOT` and `Sandbox.reset`; `RECLAIM` requires `Capability.RECLAIM`. `SandboxSpec.confined_to_guest_call_path` describes whether a kind attempts to confine changes to its call directory. It is useful host policy information, but neither proves cleanliness nor authorizes reuse. An unconfined kind can also be reused when the host explicitly accepts that risk.

**The host sets the floor.** `SandboxRouter(min_cleanup=…)` defaults to `DISPOSE`; `SandboxSpec.min_cleanup` can raise that floor, never lower it. The router selects the weakest available operation at or above the effective floor. `effective_cleanup(spec)` exposes that choice before a call runs. A host explicitly selecting `RECLAIM` gets `RESET` or `DISPOSE` if the backend cannot reclaim. Cleanup is independent of sharing scope: `IsolationScope.CALL` always disposes its own sandbox, and whole-instance cleanup drains active siblings before disposing a shared instance.

**Conformance tests measure a workload, not every future call.** `assert_nothing_left_behind` fingerprints a pristine sandbox, runs a workload and its cleanup, and compares changed paths and process identities. A difference fails the test. This can falsify a confinement claim for that workload, but passing does not establish complete confinement of arbitrary guest code. A backend without an engine diff skips the probe. [`backends/docker.md`](backends/docker.md) describes its evidence and limits.

The subject implements `FingerprintSubject.fingerprint()` in `maf_sandbox.conformance`, returning `SandboxFingerprint(changed_paths, running_programs)` or `None` when its engine cannot answer. The probe refuses a nonempty initial path diff before running the call, so an already-dirty path cannot hide a second write. Process identities distinguish a replacement from its predecessor; an unchanged keepalive process is part of the baseline. An unreadable or incomplete engine view raises and fails, including loss of the fingerprint after the call. The callback takes no arguments and must await both the workload and its cleanup on that same sandbox. The returned `ProbeResult` tuple distinguishes passing from skipping; a kind's engine test asserts that every result passed. The in-process fake implements the seam for harness tests, but its byte comparison cannot detect a rewrite restored to the original bytes and does not execute guest programs. An engine subject retains every path its diff reports, even after such a rewrite. Neither subject measures kernel state or open sockets.

**What each rung costs.** `DISPOSE` pays for deletion and the next call's create; creates on docker and wslc take about half a second. ACAS also requires cold creation: its [M6 measurement and backend decision](backends/acas.md#lifecycle) establish no consistent benefit across the shipped Python and Bicep workloads sufficient to justify snapshot reset, and both replacement strategies pay for deletion. ACAS therefore withholds `SNAPSHOT`. A backend that does establish `RESET` must restore a baseline taken before the sandbox's first input; warm reuse through `RECLAIM` requires explicit host permission and the backend's declaration.

**The backend owns removal reach; the host decides whether reuse is acceptable.** `Sandbox.reclaim(directory, *, working_directory, timeout)` remains required, but a backend withholds `RECLAIM` and refuses direct calls if it cannot establish safe removal reach. Core bounds the operand under the working directory, refuses the working directory itself and paths fewer than two components from root, and bounds cleanup time. An explicit reuse policy does not relax these reach constraints. Cleanup failures reach the existing failure policy without replacing the call result.

Three rules apply to reclamation the backend can safely attempt:

- **The caller created the directory** under `working_directory`, with an unguessable name. This distinguishes it from model-supplied input; it does not prove safe reach after the guest has run. The backend owns any safety checks and refuses if it cannot establish the reach bound.
- **A path that is not there is success.** Cleanup runs in a `finally` and must not report a second failure over the first.
- **Anything else raises**, so the caller can escalate.

**It does not replace `remove`.** `remove` takes a path a model named and stays gated by `FILES_DELETE`; `reclaim` serves framework cleanup and is admitted through `RECLAIM`. Both must satisfy their reach constraints. [WSLC](backends/wslc.md) refuses both and lets the cleanup ladder dispose the kind after each call.

**When the framework cannot clean a call, it disposes that call's sandbox.** No removal is a guarantee: an unobserved descendant can survive process cleanup and write a path back after the removal returns. A sandbox the framework could not clean is not left warm for the next call to reuse. It is disposed — `dispose_unclean(key, kind=..., instance_id=..., timeout=...)` from the same `finally`, selecting the serving backend and engine ID — and the conversation's next call starts cold. **Better a failed run than leaked data.** The ladder does not change this. A rung that reports failure escalates here as it always did, with the unclean ledger and the `FailedReclaimPolicy` unchanged; what the ladder adds is that disposal is now also a routine outcome rather than only an escalation, so the two share their machinery.

**A sandbox this router did not clean is not trusted.** The gate and the ledger live and die with the process, and `acquire` is get-or-create on every backend — so a router that starts after a host restart, or after a disposal that failed, is handed the last process's sandbox and knows nothing of what it holds. It adopts nothing on faith: the router keeps the `Sandbox.instance_id` of every sandbox it has served, and an `acquire` answering with an instance it has not seen is cleaned before the call runs, at a rung that owes nothing to what this router knows. That rules out `RECLAIM`, which owes everything to it — `reclaim` takes the one call directory this router created under an unguessable name and stops no process, so a router holding no record of the last process's calls can neither name their directories nor end their survivors. Adoption is a reset where the backend declares `SNAPSHOT` and the engine still holds the baseline, and otherwise a disposal and one fresh acquire. Acquires and adoption for each key and kind run one at a time across event loops, so concurrent first callers wait for one adoption. Reset and disposal are bounded by `ReclaimConfig.timeout`; an unsuccessful reset falls back to disposal, and a disposal that fails refuses the key unless the host chose `FailedReclaimPolicy.KEEP`. Adoption is skipped at `IsolationScope.CALL`, whose key has never served a call. Without a backend create-or-reuse signal, even a genuinely new conversation pays the first create, a disposal and a second create on backends without snapshots. The router trusts the second acquire because the disposal landed. Nothing survives a restart that the next router trusts without cleanup.

A failed directory reclamation, an unusable launcher receipt, a refused signal, an unavailable process check or observed running survivors is a cleanup failure. `FailedReclaimPolicy.DISPOSE` remains the default response; `KEEP` separately permits retaining an instance after failure. This failure setting does not authorize ordinary reuse: the host must also explicitly lower `min_cleanup`.

**Loosening is the host's, on the router, and explicit.** The same shape as the isolation floor: one keyword beside `min_isolation`, the strict posture when nothing is configured, nothing to forget. A kind cannot lower it — a kind never sees the router's construction, and `sandboxed_tool` carries no switch. A workload may ask for stricter, never for weaker.

**`on_reclaim_failure` tells the host what happened.** It runs after the disposal and says whether the disposal landed, so the guarantee never waits on a host callback's time budget. It is where a host logs, alerts or counts. It is not where safety is wired.

**A disposal that itself fails is logged loudly, and the router refuses that key** until a disposal lands — the ledger on the router that the record said to build if one were ever wanted. A sandbox that holds data nobody can remove is not served again.

**A backend says the disposal failed by returning, not by raising.** `dispose` is contractually best-effort and never raises — which is what makes it safe in a `finally`, and why reading only the raise left the refusal above unreachable: every compliant backend swallowed its delete error and was read as having disposed. It returns a `DisposalFailure` when a sandbox may still be there, or `None`, and `dispose_scope` carries the same answer beside its count as a `ScopePurge`.

**A code and a detail, not a sentence.** `DisposalCode` is a closed set — `unreachable`, `timeout`, `refused`, `unlisted`, `unknown` — so a caller deciding whether to retry, alert or escalate branches on a value this package keeps stable rather than on a backend's prose. `unknown` is always available, so no backend has to invent a more precise code to stay in the vocabulary, and several failures fold to the most actionable with every detail kept. `None` also covers a backend with no way to check: the conflation is with success, because refusing every key served by a backend that cannot answer is the wrong direction to fail in.

**The refusal carries the code, not the detail.** A host branches on `SandboxUnclean.code` — the value, not a substring of the message. The detail stays in the router's log, because a backend's sentence can carry an endpoint or a raw response body and `acquire` reaches hosts that do not sanitize. The model reads a fixed line naming no backend, id or endpoint.

## Process cleanup and observations

`host_tool_calls_over_exec` retains a versioned launcher receipt containing the program PID and optional dedicated PGID. A trusted shell helper waits at a start gate while the launcher emits the receipt and closes its control stream. Only after the backend returns the complete receipt does the host release the helper to exec the guest program with the recorded PID. Missing or invalid receipts leave the guest unreleased. Guest-writable PID and session files are diagnostic only. Cleanup uses the retained receipt on success, timeout, error and cancellation; a lost receipt never causes a fallback to those files. It collects the program result before signalling, then independently attempts transport-directory reclamation. The kind can collect work-directory artifacts before the call wrapper reclaims that directory.

The transport captures bounded Linux `/proc` snapshots before launch, after launch, before cleanup and after cleanup, through `python -I -S` using the configured interpreter. These are explicitly guest-observed diagnostics. It retains observed `(PID, start ticks)` identities for this run and physical instance, tracks observed descendants across reparenting, distinguishes preexisting and new unattributed processes, and excludes zombies from running survivors. An observed replacement is refused. Observed descendants outside the original group receive separate identity-checked signal attempts. A process appearing between snapshots is not, by itself, a signal target.

Launch observations share the run deadline. Process cleanup has one five-second budget for observation, signalling and verification; pre-signal observation can spend at most one quarter of it. Expired observations remain visible as unavailable audit records. Directory reclamation has its own bound and still runs after the process budget expires.

Four snapshots cannot find every descendant: an intermediate parent can exit before a scan, a guest can alter the collector, and numeric IDs can be reused between checking and signalling. A successful group signal or empty final snapshot is therefore not a clean-sandbox guarantee. Cgroups and stable kernel process handles are outside this design. See the [decision record](research/process-cleanup-and-reuse.md) and [process audit events](observability.md#process-observations).

## Concurrency, as the other consequence

A per-call path is tidiness, not isolation. Two calls in one conversation share one sandbox and one store; a call holding `EXEC` can read whatever its sibling is writing. The in-flight gate coordinates cleanup through one router, but does not prevent concurrent siblings from reading each other's data. A sandbox per call removes that sharing, at a cold start per call. Per-call isolation is a boundary property, declared as `IsolationScope`: a spec asks for `CALL`, a host raises every workload it serves to it with `min_isolation_scope`, and a backend that cannot create per call is refused rather than allowed to answer by sharing.

**Cleanup above `RECLAIM` waits for the last call out.** Ordinary call bodies overlap regardless of their final rung. `RECLAIM` runs at once against the call's own directory. RESET, DISPOSE and failed-reclaim escalation are recorded per serving backend and physical `instance_id`, folded to the strongest rung for each instance, and executed by the last active call. A finished call's residue stays readable by a running sibling until that sibling ends. Explicitly exclusive admission remains available where a caller must exclude every sibling. A kind asks for it with `SandboxSpec.exclusive_admission`, and the router holds every call of that kind exclusively whatever the caller passed. Such a call waits for each call ahead of it for the bound `sandboxed_tool(admission_timeout=...)` states, that call's body, plus twice that tool's own cleanup bound for reclaim or reset followed by disposal, `reclaim_timeout` where it is set and the router's otherwise; the wait restarts as each call ahead leaves and as each of its cleanup steps completes, the removal of each instance it held and then each whole-instance reset or disposal, so a call holding several instances is not charged against one budget, and a call ahead that outlasts it makes the waiter answer busy. Serialising every call would close the visibility above, because a call admitted alone reads nothing a sibling is writing, and what rules it out as a default is the cost rather than the benefit: under the `DISPOSE` default every exclusive call is the last one out and pays a disposal. The ask suits a kind whose program can reach beyond its call path: `codeact` asks, because model-written code reads whatever a sibling put in the sandbox. Ordinary kinds retain overlapping bodies to avoid paying teardown and cold start after every call; cross-router isolation and remaining sibling visibility are tracked in [#436](https://github.com/sokolaidev/maf-extensions/issues/436).

**The zero transition is a gate, not only a number.** Each `(key, kind)` entry is `serving(n)` while ordinary callers may enter, `draining(n)` from the moment an exiting call records whole-instance cleanup, and `cleaning` when the last active call leaves. The move to cleaning precedes the first cleanup await. Entrants wait during draining and cleaning; admission reopens only after every instance record succeeds or reaches the failure ledger. State is guarded by a `threading.Lock`; each waiter is notified on its own event loop. Admission and completion waits are bounded. A cancelled or expired waiter does not discard queued cleanup, and cancelled cleanup retains unfinished instance targets under the existing refusal policy. A call whose cleanup is pending waits for its result before reporting to `on_reclaim_failure`; if the completion wait expires, it reports that cleanup is still pending.

**The count is per router.** This is neither an engine lease nor coordination between routers or processes. A host serving one conversation concurrently through several routers or replicas raises `min_isolation_scope` to `CALL`, so one router's cleanup cannot delete a sandbox another router is still using.
**The fold is what a backend implements, and it is not nothing.** A per-call component in the key reaches a backend only if that backend keys sandboxes by the whole key — and all three here derive a name from `scope`, `thread_id` and `agent_dir` field by field, so a fourth field reaches none of them: both acquires resolve to one sandbox and both calls succeed. That is why the scope is declared (`BackendDeclarations.isolation_scopes`, silent meaning `CONVERSATION`) and probed (`assert_call_scope_conformance`, two subjects over keys differing only in `call_id`) rather than assumed from the key's shape.

## Where the base comes from

A backend allocates the storage base and resolves relative paths against it. `SandboxSpec.work_dir=None` delegates that choice; an explicit guest-native value keeps an image's fixed base. `guest_call_path()` returns a relative child, and the framework reclaims that child against `working_directory="."`. The same contract serves a backend with no filesystem. [`hosts.md`](hosts.md#where-the-storage-base-comes-from) describes allocation, overrides and the host-tools launcher.

## Status

| Decision | State | Tracking |
|---|---|---|
| A kind's confinement claim is measured through an engine fingerprint before its call and after cleanup | shipped — the core probe, fingerprint seam, in-process harness and package wiring check; real-engine subjects and kind adoption remain separate work | [#1004](https://github.com/sokolaidev/maf-extensions/issues/1004) (closed) by [#1027](https://github.com/sokolaidev/maf-extensions/pull/1027) (merged) |
| A call leaves nothing behind by default; cleanup is resolved from the `RECLAIM < RESET < DISPOSE` ladder, and warm reuse requires explicit host permission together with an available backend cleanup operation | shipped — core resolves the ladder and floors, cleans unfamiliar instances before use, and drains siblings before whole-instance cleanup. Docker declares RECLAIM and supplies the engine fingerprint; Bicep measures confinement and Sample 13 verifies warm reuse. Kind confinement metadata is advisory; ACAS and WSLC dispose. No shipped backend declares SNAPSHOT; ACAS deliberately withholds it after M6 | [#792](https://github.com/sokolaidev/maf-extensions/issues/792) (closed); design in [#867](https://github.com/sokolaidev/maf-extensions/pull/867) (merged), reference in [#989](https://github.com/sokolaidev/maf-extensions/pull/989) (merged), core tracked by [#979](https://github.com/sokolaidev/maf-extensions/issues/979) (closed), adoption by [#1045](https://github.com/sokolaidev/maf-extensions/pull/1045) (merged), instance cleanup by [#1060](https://github.com/sokolaidev/maf-extensions/pull/1060) (merged), sibling gate by [#1065](https://github.com/sokolaidev/maf-extensions/pull/1065) (merged), Bicep by [#1068](https://github.com/sokolaidev/maf-extensions/pull/1068) (merged), samples by [#1078](https://github.com/sokolaidev/maf-extensions/pull/1078) (merged), ACAS decision by [#1035](https://github.com/sokolaidev/maf-extensions/pull/1035) (merged), explicit host reuse under [#463](https://github.com/sokolaidev/maf-extensions/issues/463) (closed) by [#1091](https://github.com/sokolaidev/maf-extensions/pull/1091) (merged) |
| The call owns a guest path; the per-call record, and the `finally` that reclaims it | shipped | [#496](https://github.com/sokolaidev/maf-extensions/pull/496), kinds wired in [#500](https://github.com/sokolaidev/maf-extensions/pull/500), sample 15 aligned in [#499](https://github.com/sokolaidev/maf-extensions/pull/499) |
| A host learns of a reclaim that did not happen | shipped — `on_failure` and `timeout` on `ReclaimConfig` are the router's defaults (`SandboxRouter(reclaim=ReclaimConfig(...))`), with `on_reclaim_failure` and `reclaim_timeout` as per-tool overrides on `sandboxed_tool`, so a host on a packaged kind sets the policy once on the router and every kind inherits it. It runs **after** the framework has already acted, and `ReclaimFailure.disposal` reports which of `disposed`, `failed` or `kept` it did, so the callback is where a host logs, alerts or counts and not where it decides: safety is the router's, and `ReclaimConfig(failed_reclaim_policy=FailedReclaimPolicy.KEEP)` is the only opt-down. Its own exception is logged and swallowed. The worked policy is `samples/07_docker_diagram`: it states a `failed_reclaim_policy` rather than inheriting it, logs from `on_failure` to stderr because `ReclaimFailure.path` is host-side detail, and reports the handler's own count — which its live check requires to be nought, because a healthy run on a working backend reclaims every call directory. The handler *firing* is `samples/12_purge_lifecycle` acts 5 to 8, on a real container rather than a fake: `DockerSandboxConfig(cap_drop_all=True)` leaves the container's root without `CAP_DAC_OVERRIDE`, so a guest program that removes write permission from a directory it created defeats `rm -rf` as root and as the image's user alike, and the removal fails for the reason the engine gives. All three `disposal` outcomes are reached — act 5 takes the default and the escalation disposal is counted with `docker ps`, act 6 takes `KEEP` and counts the container that stays, and act 7 starves the cleanup budget through `sandboxed_tool`'s per-tool `reclaim_timeout` so the remedy is unproven too, reports `failed`, and has the key refused with `SandboxUnclean` on the next acquire. Act 8 raises from the handler and shows it contained: the call's answer is the body's, and the record the handler wrote before raising survives. What the host was told is recorded through `maf-sandbox-otel` and a handler of its own — which is also where the exported telemetry's limit shows: `maf_sandbox.call.unclean` counts transport notes about processes and reads nought here, and the disposal records do not distinguish the escalation from the router's own adoption cleanup, so the path, the reason and the `disposal` outcome exist in the host's record alone | [#520](https://github.com/sokolaidev/maf-extensions/issues/520), the router default and policy in [#677](https://github.com/sokolaidev/maf-extensions/issues/677) and [#714](https://github.com/sokolaidev/maf-extensions/pull/714), the handler exercised under [#760](https://github.com/sokolaidev/maf-extensions/issues/760) (closed) by [#1140](https://github.com/sokolaidev/maf-extensions/pull/1140) (merged) |
| A call's directory can be removed by whoever will be asked to remove it | shipped — `write_file` used to create it with the backend's own authority while `exec` ran as the image's user, so on a non-root image the removal refused and every call left its files behind. Docker's `write_file` now carries the image user's uid/gid on its tar entries (and stamps the call-directory parents explicitly, since implicit intermediates land root-owned), so the call directory is the guest's and the guest's own `rm` can take it. The reach rule still bounds the removal's authority, and since the write half landed that bound costs nothing on the images this suite runs — the guest-owned call directory is one the guest can empty. Docker refuses a spec requiring `FILES_OUT` or `HOST_TOOLS` when its uid cannot be resolved, before a call directory is written. Other workloads retain the root-owned fallback with a warning; their guest-authority reclaim may still fail. Unresolved facts are retried on the next acquire ([#741](https://github.com/sokolaidev/maf-extensions/issues/741) (closed) by [#1031](https://github.com/sokolaidev/maf-extensions/pull/1031) (merged)). WSLC now refuses raised reclamation and uses per-kind disposal ([#982](https://github.com/sokolaidev/maf-extensions/issues/982) (closed) by [#1036](https://github.com/sokolaidev/maf-extensions/pull/1036) (merged)). ACAS also refuses direct reclamation because safe ancestry for a host-authority delete cannot be established; its router-managed cleanup uses disposal | [#680](https://github.com/sokolaidev/maf-extensions/issues/680), docker's removal authority in [#684](https://github.com/sokolaidev/maf-extensions/pull/684), reach rule in [#686](https://github.com/sokolaidev/maf-extensions/pull/686), wslc in [#706](https://github.com/sokolaidev/maf-extensions/pull/706), acas in [#707](https://github.com/sokolaidev/maf-extensions/pull/707), tracking in [#695](https://github.com/sokolaidev/maf-extensions/issues/695) |
| The framework disposes a sandbox it could not clean — a failed reclaim or observed incomplete process cleanup — and the host loosens that on the router | shipped — `sandboxed_tool` disposes from its `finally` before `on_reclaim_failure` runs, `ReclaimConfig(failed_reclaim_policy=FailedReclaimPolicy.KEEP)` is the opt-down, and a disposal that does not land makes the router refuse the key with `SandboxUnclean` | [#617](https://github.com/sokolaidev/maf-extensions/issues/617) |
| A backend reports a delete that failed without raising, so the refusal fires on a silent failure | shipped — `dispose` returns a `DisposalFailure` (a `DisposalCode` to branch on, a detail that stays in the log) and `dispose_scope` a `ScopePurge`; the router reads both beside the raise, keeps a conversation's keys refused when its purge did not land, and quotes the code back in `SandboxUnclean`; all four in-tree backends report | [#641](https://github.com/sokolaidev/maf-extensions/issues/641) |
| A required `Sandbox.reclaim`; core dispatches admitted reclamation rather than spelling it | shipped — all four in-tree backends implement the member; unsafe mechanisms refuse, and the `RECLAIM` declaration gates both cleanup selection and the conformance suite | [#477](https://github.com/sokolaidev/maf-extensions/issues/477) |
| Cleanup and retries select physical instances and preserve sibling instances | implemented — routine disposal is ledger-free; failed cleanup retains backend/kind/instance targets and attempt identity while refusing the whole key. The last-call gate folds pending cleanup to the strongest rung per serving backend and instance | [#1051](https://github.com/sokolaidev/maf-extensions/issues/1051) (closed) by [#1060](https://github.com/sokolaidev/maf-extensions/pull/1060) (merged), gate in [#1052](https://github.com/sokolaidev/maf-extensions/issues/1052) (closed) by [#1065](https://github.com/sokolaidev/maf-extensions/pull/1065) (merged) |
| Whole-instance cleanup waits for active siblings without serialising ordinary call bodies | implemented — the serving/draining/cleaning gate covers routine cleanup and reclaim escalation across event loops using one router. The lock this issue proposed is decided against: the gate orders cleanup, never bodies, and under the `DISPOSE` default a serialised call would dispose after every call. Explicit exclusive admission remains available on the router's `enter_call`, and a kind asks for it on its spec; multiple routers or processes require CALL scope; a sibling reading a sibling's files is [#436](https://github.com/sokolaidev/maf-extensions/issues/436) (open) | [#476](https://github.com/sokolaidev/maf-extensions/issues/476) (closed) by [#1127](https://github.com/sokolaidev/maf-extensions/pull/1127) (merged), parent [#979](https://github.com/sokolaidev/maf-extensions/issues/979) (closed), gate in [#1052](https://github.com/sokolaidev/maf-extensions/issues/1052) (closed) by [#1065](https://github.com/sokolaidev/maf-extensions/pull/1065) (merged) |
| A kind asks for exclusive admission on its spec, and `codeact` asks | implemented — `SandboxSpec.exclusive_admission` is honoured by `enter_call` on every path, `sandboxed_tool(admission_timeout=...)` bounds the wait per call ahead and the bound restarts as each leaves, and codeact sets both, the bound from `exec_timeout_seconds` | [#1129](https://github.com/sokolaidev/maf-extensions/issues/1129) (closed) by [#1134](https://github.com/sokolaidev/maf-extensions/pull/1134) (merged) |
| A workload can demand a sandbox per call, and a wiring that cannot give it one is refused | shipped — core has the axis (`SandboxSpec(isolation_scope=IsolationScope.CALL)`, `SandboxRouter(min_isolation_scope=…)` as the host's floor, `SandboxKey.call_id` filled by `SandboxToolSession.key()` from the same id the guest path uses, and a `finally` that deletes the sandbox instead of emptying it — `FailedReclaimPolicy.KEEP` does not loosen that one, because the delete is the separation rather than an escalation over it), and all three backends now serve it: `call_id` reaches whatever names a sandbox — the container name on docker and wslc, the registry entry and the disposal's label selector on all three — and each answers `assert_call_scope_conformance`. A conversation-scoped key keeps the name and the labels it already had, so the release that adds the scope orphans nothing. A delete that does not land is reported and not marked unclean: the ledger refuses a key's *next* acquire and a call-scoped key has none, so what leaks is a sandbox no later call can address, left to the conversation's purge — which selects on scope and thread, never on the call, and so still reaches it. Erasure is still not claimed — an unlink is not an overwrite, and a snapshotted image can keep the blocks | [#436](https://github.com/sokolaidev/maf-extensions/issues/436) (closed) |
| A backend allocates the storage base and resolves every path against it | implemented — backend allocation, relative addressing and image overrides | preparation in [#466](https://github.com/sokolaidev/maf-extensions/issues/466) (closed) by [#1086](https://github.com/sokolaidev/maf-extensions/pull/1086) (merged); allocation in [#480](https://github.com/sokolaidev/maf-extensions/issues/480) (closed) by [#1090](https://github.com/sokolaidev/maf-extensions/pull/1090) (merged) |
| `SandboxToolSession` is renamed `SandboxToolBinding`, once the semantics settle | open | untracked |
