name: Verify (live)

# Installs the PUBLISHED wheels into a clean environment and runs the samples below, each
# asserting that its own workload ran rather than that the program exited zero.
#
# **One job is not a sample and does not install published wheels**: `acas-e2e` runs
# `maf-sandbox-acas`'s live suite from this checkout. Everything else here asks "does the
# published set still work"; that one asks "does the real service still do what the backend
# believes" — refusals, confinement, teardown read back from the service — which a sample with a
# model in the loop cannot ask (#306). It is listed here rather than in its own workflow because
# this is where the `live-verify` environment, the OIDC login and the billable-run gating already
# live, and splitting it would duplicate all three.
#
# **No check here can be satisfied by the model's prose.** A sample that judges a tool's answer
# prints a fenced block of what the tool returned, closed by a line tagged `[measured]`, and its
# check reads that. A sample that judges a landed file prints no block — it reads the file, and
# off the tag it reads what the host counted: what the router disposed, and for 08 and 14 what
# the sink took. Several checks additionally require the answer to appear in the reply, which is
# a claim about the round trip reaching the model and is never the condition carrying a verdict.
# The alternative was wrong in both directions: a model that never called the tool could satisfy
# a check made over prose, and three healthy releases went red over a run writing `**error**`
# where the pattern wanted `[error]` (#314).
#
# Half are paired: one workload against a billable Azure microVM and the same workload against
# a container, so a red on one side alone names the backend. 01 and 05 report the compiler's
# diagnostics; 03 and 06 return the 100th Fibonacci number from code run in the sandbox; 08 and
# 14 land a declared summary on disk and gate on the file, not on the account of it. 07 lands one
# too, through a kind written in the sample instead of the packaged one, and pairs with 08 on
# this side of the machine rather than across backends. 09 runs 01's bicep workload against the
# CLI on the runner itself, so it pairs with nothing and names no backend. 13 is the only job
# that runs more than one turn: a model writes a file, the compiler reports on it, and a second
# turn repairs it against the same sandbox.
#
# This is the half of issue #33 that runs after a release rather than on a pull request. The
# ACAS jobs each create a billable Azure sandbox and need a subscription; the docker jobs create
# a container on the runner and need no sandbox configuration; 09 needs neither. **A job that
# calls no model and reads no variable carries no `environment:` and no `permissions:` block**,
# because it mints no token — stated as the rule rather than a list, which is what went stale
# here before. It runs on demand, and once after each release that could change what a sample
# exercises (dispatched by publish-packages.yml).
#
# Samples 02 and 04 are the two with no job, and structurally: `wslc` needs Windows with a WSL
# that ships it, which the Linux runners are not.
#
# Authentication is OIDC federation to Azure — no stored secret, matching how publishing works.
# Everything each sample reads is non-secret configuration (endpoints and ids), held as
# environment variables on the `live-verify` environment. The ACAS jobs read the sandbox
# configuration as well as the model's; every other job that calls a model reads only the
# model's, whether its guest is built here or pulled; a job that calls no model reads nothing.
# See docs/maintainers.md.
#
# PEP 723 metadata is the single dependency source; `--no-project` ensures uv resolves its
# published packages rather than this checkout's workspace.
on:
  workflow_dispatch:
    inputs:
      package:
        description: "Package whose release to verify (empty runs every sample)"
        required: false
        type: string
        default: ""
  workflow_call:
    inputs:
      package:
        description: "Package whose release to verify (empty runs every sample)"
        required: false
        type: string
        default: ""

# Key by package because GitHub retains only one pending run per concurrency group; a global group
# silently drops checks from parallel package releases. Keep same-package runs serialized and never
# cancel an in-flight run so it can dispose its sandbox itself (#325).
concurrency:
  group: verify-live-${{ inputs.package || 'all' }}
  cancel-in-progress: false

permissions:
  contents: read

# Where the `scripts/check_live_*.py` assertions are read from, which is not where the samples
# are read from. The samples are the thing under test and belong at the ref that triggered the
# run — for a post-release call that is the release tag, so they are the code that shipped. The
# checks are the *test*, and a test has no business being frozen with its subject: a tagged run
# would otherwise assert with the checker as it stood at the tag, so a fix to it could never
# reach a release already cut, and a checker bug found mid-train keeps costing for the rest of
# that train. `.` for any other ref, so dispatching from a branch still runs that branch's
# checks — which is how a change to one gets tried out. (#318.)
env:
  HARNESS: ${{ startsWith(github.ref, 'refs/tags/') && '.harness' || '.' }}

jobs:
  sample-01:
    name: samples/01_acas_bicep against a live sandbox
    # Empty input (verify-everything) or a package sample-01 actually exercises: the router
    # (maf-sandbox) and the backend it runs on (maf-sandbox-acas) both affect it, and so does
    # its own package (maf-sandbox-bicep); a maf-sandbox-codeact or maf-sandbox-wslc release
    # cannot change anything this sample does.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-acas", "maf-sandbox-bicep"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Half of the federated-credential subject Azure checks, and where the non-secret sandbox
    # and model configuration lives. `live-verify` is deliberately ungated: the post-release
    # call must not wait on a human, and there is no credential to protect while it does.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # Fail early and by name if the environment was never configured — azure/login's own
      # error for an empty client-id does not say which field is missing.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          ACAS_SANDBOX_REGISTRY: ${{ vars.ACAS_SANDBOX_REGISTRY }}
          BICEP_SANDBOX_IMAGE: ${{ vars.BICEP_SANDBOX_IMAGE }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            ACAS_SANDBOX_ENDPOINT ACAS_SANDBOX_SUBSCRIPTION_ID ACAS_SANDBOX_RESOURCE_GROUP \
            ACAS_SANDBOX_GROUP ACAS_SANDBOX_REGISTRY BICEP_SANDBOX_IMAGE \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Runs the sample as a consumer would, then asserts on the block it prints from what
      # bicep_validate returned. The sample authenticates with DefaultAzureCredential, which
      # the az login above satisfies — the AZURE_CLIENT_ID/TENANT/SUBSCRIPTION values are login
      # inputs only and are deliberately not exported here, so DefaultAzureCredential resolves
      # cleanly through the CLI rather than a half-populated EnvironmentCredential.
      - name: Run sample 01 and assert the diagnostics came back
        env:
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          ACAS_SANDBOX_REGISTRY: ${{ vars.ACAS_SANDBOX_REGISTRY }}
          BICEP_SANDBOX_IMAGE: ${{ vars.BICEP_SANDBOX_IMAGE }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/01_acas_bicep/agent.py | tee /tmp/sample-out.txt
          # stdlib-only, so the runner's own python3 runs it — uv run's env is not on its path.
          python3 "$HARNESS"/scripts/check_live_sample.py /tmp/sample-out.txt

  sample-03:
    name: samples/03_acas_codeact against a live sandbox
    # No `needs: sample-01` — the two never had a data dependency (each creates its own
    # sandbox and reads disjoint configuration); the `needs` edge existed only to keep this
    # run's billable sandboxes from being up at the same time. That trade is deliberately
    # flipped now: the jobs run concurrently, so for the few minutes the ACAS samples are live
    # their sandboxes are up at once, in exchange for the run taking one sample's time instead
    # of all of them in turn. A failure still names which sample broke.
    # Four per run since sample-15 joined — 01, 03, 14 and 15 — and four per run is not four
    # overall: the workflow-level `concurrency` block above is keyed per package, so a release
    # train verifying several packages has several runs in flight and each holds its own. What
    # each package costs is the number of billable jobs its `if` admits: `maf-sandbox` and
    # `maf-sandbox-acas` five (01, 03, 14, 15 and `acas-e2e`), `maf-sandbox-codeact` three (03,
    # 14 and 15),
    # `maf-sandbox-bicep` one (01), everything else none. Count sandboxes rather than jobs, since
    # `acas-e2e` uses two — one shared by its probes, one the lifecycle test disposes as the thing
    # under test — and so does sample-15, a sandbox per route. So a train releasing all four
    # peaks at nineteen concurrent billable sandboxes for the twenty minutes the timeout
    # allows, where it was thirteen before sample 15, nine before the live suite and six
    # before sample 14. Total spend is unchanged by the concurrency; only the peak moved. (#325.)
    # Empty input (verify-everything) or a package sample-03 actually exercises: the router
    # (maf-sandbox) and the backend it runs on (maf-sandbox-acas) both affect it, and so does
    # its own package (maf-sandbox-codeact); a maf-sandbox-bicep or maf-sandbox-wslc release
    # cannot change anything this sample does.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-acas", "maf-sandbox-codeact"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Half of the federated-credential subject Azure checks, and where the non-secret sandbox
    # and model configuration lives. `live-verify` is deliberately ungated: the post-release
    # call must not wait on a human, and there is no credential to protect while it does.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # Fail early and by name if the environment was never configured — azure/login's own
      # error for an empty client-id does not say which field is missing, which is the whole
      # reason the login triple is checked here too and not only the six variables
      # samples/03_acas_codeact itself reads. No registry and no image variable among those
      # six: CODEACT_IMAGE is a hardcoded, fully-qualified MCR reference in the sample.
      # The triple is checked and deliberately *not* exported to the run step below — see the
      # note there about EnvironmentCredential.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            ACAS_SANDBOX_ENDPOINT ACAS_SANDBOX_SUBSCRIPTION_ID ACAS_SANDBOX_RESOURCE_GROUP \
            ACAS_SANDBOX_GROUP AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Runs the sample as a consumer would, then asserts on the interpreter's own stdout,
      # which the sample prints from the tool result rather than from the reply. The sample
      # authenticates with DefaultAzureCredential, which the az login above satisfies — the
      # AZURE_CLIENT_ID/TENANT/SUBSCRIPTION values are login inputs only and are deliberately
      # not exported here, so DefaultAzureCredential resolves cleanly through the CLI rather
      # than a half-populated EnvironmentCredential.
      - name: Run sample 03 and assert the computed answer came back
        env:
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/03_acas_codeact/agent.py | tee /tmp/sample03-out.txt
          # stdlib-only, so the runner's own python3 runs it — uv run's env is not on its path.
          python3 "$HARNESS"/scripts/check_live_codeact_sample.py /tmp/sample03-out.txt

  sample-05:
    name: samples/05_docker_bicep against a live container
    # Sample 01's workload and its `main.bicep`, in a Docker container on this runner instead of
    # a billable Azure microVM — so the assertion is sample 01's too, and a red here while
    # sample-01 is green is a statement about the backend rather than about the compiler. Runs
    # for the router (maf-sandbox), the backend (maf-sandbox-docker) and its own kind
    # (maf-sandbox-bicep); an acas, codeact or wslc release cannot change what this sample does.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-bicep", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # For the model only: `live-verify` holds the non-secret Azure OpenAI configuration and is
    # half the federated-credential subject. The image this sample runs is built below rather
    # than configured, so there is no sandbox variable to read.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Where samples 06 and 08 run an MCR image that `docker run` pulls, this one's guest is
      # built from this repository — the same context sample 01 imports into an Azure sandbox
      # group, so the two jobs compile with one Bicep CLI and one rule set. The tag is local and
      # unqualified because the backend runs what is already on the machine.
      - name: Build the sandbox image
        run: docker build -t bicep-sandbox:local images/bicep-sandbox

      - name: Run sample 05 and assert the diagnostics came back
        env:
          BICEP_SANDBOX_IMAGE: bicep-sandbox:local
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/05_docker_bicep/agent.py | tee /tmp/sample05-out.txt
          # Sample 01's assertion, unchanged: the same file compiled by the same CLI produces
          # the same two rule ids, whatever ran it — and both samples print them the same way.
          python3 "$HARNESS"/scripts/check_live_sample.py /tmp/sample05-out.txt

  sample-06:
    name: samples/06_docker_codeact against a live container
    # The same task and assertion as sample-03, but the sandbox is a Docker container on this
    # runner rather than a billable Azure microVM — so this job needs no sandbox subscription
    # and creates no billable resource; the only cloud dependency is the model, reached with a
    # federated credential. Runs for an empty input or a package it actually exercises: the
    # router (maf-sandbox), the backend (maf-sandbox-docker) and its own kind (maf-sandbox-
    # codeact); an acas, bicep or wslc release cannot change what this sample does.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-codeact", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # For the model only: `live-verify` holds the non-secret Azure OpenAI configuration and is
    # half the federated-credential subject. There is no sandbox configuration to hold — the
    # docker backend reads none.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # The federation inputs azure/login needs and the two model variables the sample reads —
      # there is no sandbox configuration for this backend. azure/login's own error for an empty
      # client-id does not say which field is missing, so this checks the login variables too,
      # by name, before the login step reaches them.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # ubuntu-latest ships a running Docker daemon, so the sandbox is free and local. The
      # sample authenticates the model with DefaultAzureCredential, which the az login above
      # satisfies — the AZURE_CLIENT_ID/TENANT/SUBSCRIPTION values are login inputs only and are
      # deliberately not exported here, so DefaultAzureCredential resolves through the CLI.
      - name: Run sample 06 and assert the computed answer came back
        env:
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/06_docker_codeact/agent.py | tee /tmp/sample06-out.txt
          # The same assertion as sample 03: same task, same one right answer, same printed shape.
          python3 "$HARNESS"/scripts/check_live_codeact_sample.py /tmp/sample06-out.txt

  sample-07:
    name: samples/07_docker_diagram against a live container
    # The pull surface driven by a kind defined in the sample rather than installed from a
    # package — `sandboxed_tool`, `collect_outputs`, `OutputSink` and `DeclaredOutput` used the
    # way a third party writing a kind uses them. That is what this job gates and sample-08
    # does not. No kind package is involved, so a codeact, acas, bicep or wslc release cannot
    # change what it does; the router and the backend can.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # For the model only, as in sample-05: the guest image is built below, not configured.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Graphviz on a Debian base, built from this repository like sample-05's guest. Nothing
      # pushes it anywhere: the backend runs what is on the machine.
      - name: Build the sandbox image
        run: docker build -t diagram-sandbox:local images/diagram-sandbox

      - name: Run sample 07 and assert a rendered PNG came back out
        env:
          DIAGRAM_SANDBOX_IMAGE: diagram-sandbox:local
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          # As in sample-08: an image left behind by an earlier run would satisfy the on-disk
          # half without this run having rendered anything.
          rm -rf samples/07_docker_diagram/out
          uv run --no-project samples/07_docker_diagram/agent.py | tee /tmp/sample07-out.txt
          # The model's prose is not evidence a renderer ran, so the assertion is the disposal
          # line and the PNG itself.
          python3 "$HARNESS"/scripts/check_live_diagram_sample.py \
            /tmp/sample07-out.txt samples/07_docker_diagram/out/diagram.png

  sample-08:
    name: samples/08_docker_codeact_files against a live container
    # Both of the packaged CodeAct kind's file channels, in one turn: a file store file in and a
    # declared output back out. Sample-07 gates the same pull surface through a hand-written
    # kind; this one gates what an adopter reaches for first, the channels the package itself
    # exposes. Same runner economics as sample-06: a Docker container costs nothing and the
    # model is reached with a federated credential. Runs for the same three packages, since it
    # is the same router, backend and kind.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-codeact", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Run sample 08 and assert a file went in and a file came out
        env:
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          # A fresh checkout has no `out/`, but deleting it is what makes the on-disk half of
          # the assertion mean anything: a summary left by an earlier run would pass it without
          # this run having landed a thing.
          rm -rf samples/08_docker_codeact_files/out
          uv run --no-project samples/08_docker_codeact_files/agent.py | tee /tmp/sample08-out.txt
          # Two halves, and the second is the point: the right total in stdout, and the declared
          # summary actually on disk under its declared name.
          python3 "$HARNESS"/scripts/check_live_codeact_files_sample.py \
            /tmp/sample08-out.txt samples/08_docker_codeact_files/out/summary.md

  sample-09:
    name: samples/09_inprocess_bicep against the host
    # The one sample whose backend is not a sandbox at all: a backend the sample defines
    # (`NoIsolationBackend` in `no_isolation_backend.py`) that shells out to the real bicep CLI on this runner.
    # No container, no VM, no image — but the bicep binary has to be on PATH, so this job installs
    # it. It creates no billable resource; the only cloud dependency is the model, reached with a
    # federated credential the way sample-06 does. Runs for an empty input or a package it actually
    # exercises: the router lives in maf-sandbox and the workload in maf-sandbox-bicep; a codeact,
    # acas, docker or wslc release cannot change what this sample does.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-bicep"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # For the model only, as in sample-06: `live-verify` holds the non-secret Azure OpenAI
    # configuration and is half the federated-credential subject. There is no sandbox
    # configuration to hold — the backend runs the bicep binary already on this runner.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # The backend shells out to the real bicep CLI on this runner, so the binary must be on
      # PATH — the container images bake it in (`images/bicep-sandbox/Dockerfile` pins v0.46.1);
      # this job installs the same version onto the runner. `bicep --version` fails fast if the
      # runner is missing an ICU the binary needs, rather than failing later inside the workload.
      - name: Install the bicep CLI
        run: |
          set -euo pipefail
          curl -fsSL -o /tmp/bicep \
            "https://github.com/Azure/bicep/releases/download/v0.46.1/bicep-linux-x64"
          sudo install -m 0755 /tmp/bicep /usr/local/bin/bicep
          bicep --version

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # No container to build and no sandbox to configure — the backend runs the bicep binary on
      # this runner. The sample authenticates the model with DefaultAzureCredential, which the az
      # login above satisfies; the AZURE_CLIENT_ID/TENANT/SUBSCRIPTION values are login inputs
      # only and are deliberately not exported here, so DefaultAzureCredential resolves through
      # the CLI.
      - name: Run sample 09 and assert the diagnostics came back
        env:
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/09_inprocess_bicep/agent.py | tee /tmp/sample09-out.txt
          # Sample 01's assertion, unchanged: the same file compiled by the same CLI produces the
          # same two rule ids, whatever ran it. stdlib-only, so the runner's own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_sample.py /tmp/sample09-out.txt

  sample-10:
    name: samples/10_inprocess_host_tools against the published wheels
    # The router's own posture surface — the host-tools registry, the aggregate it folds to,
    # and the two deny axes — so only a maf-sandbox release can change what this exercises. No
    # backend package is involved: the sample builds the one backend it needs from
    # `maf_sandbox.testing`, because no shipped backend declares Capability.HOST_TOOLS.
    if: inputs.package == '' || inputs.package == 'maf-sandbox'
    runs-on: ubuntu-latest
    # A second of arithmetic, not twenty minutes of sandbox. The cap is here to catch a hang,
    # not to allow for work.
    timeout-minutes: 5
    # No `environment:` and no `permissions:` block, and both absences are the point. This
    # sample reads no configuration and calls no service at run time, so there is
    # nothing in `live-verify` for it and no federated credential to mint. (It is not
    # offline — checkout and `uv`'s resolve reach the network, as in every job here.) — the workflow-level
    # `contents: read` is the whole of what it needs. It is the cheapest job here: sample-12
    # also calls no model, and every one of the other ten pays for inference at least, of which
    # three pay for a billable microVM as well (01, 03, 14).
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # No configuration check step either: there is no variable to be missing. A sample that
      # cannot be half-configured cannot fail the way the others are guarded against.
      - name: Run sample 10 and assert the contract held
        run: |
          set -euo pipefail
          uv run --no-project samples/10_inprocess_host_tools/agent.py | tee /tmp/sample10-out.txt
          # Strict where its siblings are loose: no model stands between the library and
          # stdout, so the printed values are the library's own answers rather than a
          # retelling of them. stdlib-only, so the runner's own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_host_tools_sample.py /tmp/sample10-out.txt

  sample-11:
    name: samples/11_router_two_backends against a live container
    # The router's own job — which backend serves, what it refuses, what it lets a workload
    # reach, and who disposal reaches. Acts 1, 2, 3 and 5 exercise the router (maf-sandbox) and
    # the backend it selects (maf-sandbox-docker) and nothing else. Act 4 adds the Bicep kind,
    # because the only honest way to show egress deciding an outcome is a workload that cannot
    # do its job without the network it asked for — so a maf-sandbox-bicep release can change
    # what this sample does, and the allowlist it runs under is that package's, not the
    # sample's.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-bicep", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    # Two container runs with a real `br/public` module restore in one of them, plus two model
    # turns. The cap is here to catch a hang, not to allow for work.
    timeout-minutes: 25
    # For the model only, as in sample-05: `live-verify` holds the non-secret Azure OpenAI
    # configuration and is half the federated-credential subject. Both images this sample runs
    # are built below rather than configured, so there is no sandbox variable to read.
    #
    # This job used to have neither block, because sample 11 used to call no model. Act 4 is
    # why it does now, and the cost is real: two inference turns where there were none. It buys
    # the one thing a unit test cannot say — that a deployment's egress posture decides whether
    # real work succeeds — and the containers it creates are still free.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Act 4's guest, built from this repository like sample-05's — the same Bicep CLI and the
      # same `bicepconfig.json`, so a difference between the two jobs is about egress rather
      # than about the compiler. The tag is local and unqualified because the backend runs what
      # is already on the machine.
      - name: Build the sandbox image
        run: docker build -t bicep-sandbox:local images/bicep-sandbox

      # Act 4 enforces an allowlist and needs something to enforce it with. Built from the
      # *published* package's own recipe, the way the sample tells a reader to — `uv run
      # --no-project` resolves maf-sandbox-docker from the index, and `proxy_build_context()`
      # points at the copy inside it, so this builds what a reader would build rather than
      # this checkout's source. Without the image the act skips itself, and a skipped
      # allowlist reads exactly like an enforced one; the check refuses that.
      - name: Build the egress proxy from the published package
        run: |
          set -euo pipefail
          context="$(uv run --no-project --with maf-sandbox-docker python -c \
            'from maf_sandbox_docker import proxy_build_context; print(proxy_build_context())')"
          docker build -t maf-egress-proxy:ci "$context"

      - name: Run sample 11 and assert the router chose, refused, confined and disposed
        env:
          BICEP_SANDBOX_IMAGE: bicep-sandbox:local
          MAF_EGRESS_PROXY_IMAGE: maf-egress-proxy:ci
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/11_router_two_backends/agent.py | tee /tmp/sample11-out.txt
          # Strict, like sample 10's, and for the same reason: every line the check reads
          # carries the `[measured]` tag, which the sample takes away from anything the model
          # said before printing it. Two assertions carry the job — the disposal count, 2 when
          # only one of the two registered backends was serving, and act 4's restore pair,
          # where the module must fail to restore under `closed` and succeed under the
          # allowlist, because either verdict alone is consistent with a sandbox that confines
          # nothing. stdlib-only, so the runner's own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_router_sample.py /tmp/sample11-out.txt

  sample-12:
    name: samples/12_purge_lifecycle against a live container
    # The lifecycle end no other sample reaches: reuse within a turn, `router.scope` at the end
    # of one, and `SandboxPurger` on a thread delete. Only the router (maf-sandbox) and the
    # backend it disposes through (maf-sandbox-docker) can change what this exercises; no kind
    # package is involved.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # No `environment:` and no `permissions:`, as for samples 10 and 11: this one calls no model
    # either. It does create containers — five threads' worth, one at a time — and counts them
    # with `docker ps`, which is the only evidence a purge claim is worth anything.
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Run sample 12 and assert every sandbox was reclaimed
        run: |
          set -euo pipefail
          uv run --no-project samples/12_purge_lifecycle/agent.py | tee /tmp/sample12-out.txt
          # Two assertions carry it: no container left behind, and the purger finding 1 on the
          # abandoned thread — a purger wired to nothing reports 0 everywhere, so the tidy
          # thread's expected 0 cannot be the only zero checked. stdlib-only, so the runner's
          # own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_purge_sample.py /tmp/sample12-out.txt

  sample-13:
    name: samples/13_bicep_fix_loop against a live container
    # The only sample that runs more than one turn, and the only one whose store starts empty:
    # the model writes `main.bicep` in turn 1 and repairs it in turn 2, both against one
    # sandbox. Sample 05's workload, arrived at by a brief rather than checked in. Runs
    # for the router (maf-sandbox), the backend it acquires through (maf-sandbox-docker) and the
    # kind that supplies the tool (maf-sandbox-bicep) — the same three sample 05 answers to,
    # because it is the same stack with a second turn on top.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-bicep", "maf-sandbox-docker"]'), inputs.package)
    runs-on: ubuntu-latest
    # Longer than sample 05's 20: two model turns, and the fix turn does several tool calls of
    # its own before it answers.
    timeout-minutes: 25
    # For the model only, as in sample 05 — `live-verify` holds the non-secret Azure OpenAI
    # configuration and is half the federated-credential subject. The image is built below.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Built here rather than pulled, exactly as sample 05 does it, and the freshness matters
      # more for this sample than for that one: `bicepconfig.json` lives in the image, so a
      # stale one reports fewer diagnostics at their built-in severities and the fix turn is
      # handed a smaller problem — a run that goes green having tested less than it says.
      - name: Build the sandbox image
        run: docker build -t bicep-sandbox:local images/bicep-sandbox

      - name: Run sample 13 and assert the file was repaired against one sandbox
        env:
          BICEP_SANDBOX_IMAGE: bicep-sandbox:local
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          # Three claims, and the check needs all of them: turn 1 wrote a file with a real fault
          # in it, one container served all four acquires, and the compiler agrees the file
          # turn 2 left is repaired. Any one alone passes on a broken sample — perfect reuse of
          # a sandbox that fixed nothing, or a fix that quietly booted a second container to
          # make it. stdlib-only, so the runner's own python3 runs it.
          #
          # Retry only exit 3, which marks model-only non-convergence; deterministic failures exit.
          # Annotate retries and summarize attempts so intermittent failures remain visible.
          attempts=0
          status=0
          while [ "$attempts" -lt 2 ]; do
            attempts=$((attempts + 1))
            # Captured rather than left to `set -e`: a sample that dies here would otherwise
            # take the whole step with it, before the attempt count is written. It is also not
            # the model's half — nothing was measured — so it breaks without a second loop.
            status=0
            uv run --no-project samples/13_bicep_fix_loop/agent.py | tee /tmp/sample13-out.txt || status=$?
            if [ "$status" -ne 0 ]; then
              echo "::error title=sample 13 did not run::the sample exited $status, so the check saw nothing"
              break
            fi
            python3 "$HARNESS"/scripts/check_live_fix_loop_sample.py /tmp/sample13-out.txt || status=$?
            [ "$status" -eq 3 ] || break
            if [ "$attempts" -lt 2 ]; then
              note="the model's half did not converge on attempt $attempts and every other"
              note="$note measurement passed, so the two-turn loop runs once more (#421)"
              echo "::warning title=sample 13 retried::$note"
            fi
          done
          said="samples/13_bicep_fix_loop: exit $status after $attempts attempt(s), 2 allowed."
          echo "$said" | tee -a "$GITHUB_STEP_SUMMARY"
          exit "$status"

  sample-14:
    name: samples/14_acas_codeact_files against a live sandbox
    # The pull surface on a second backend, which is the whole reason this job exists (#300).
    # Sample 08 is its pair and the assertion is shared, so a red here while 08 is green is a
    # statement about ACAS rather than about the channels: same task, same sales.csv, same two
    # measured lines, a different backend underneath. Reading a file back out of *this* backend is
    # a stat and a read over a preview control plane, where a mocked test is least likely to
    # match reality — #139 and #142 both turned on the difference between what the SDK reported
    # and what the payload carried, and until this job the pull surface here was exercised by
    # nothing live.
    #
    # **The third billable job in this workflow**, and the reason to accept a third: the two that
    # existed run a command and read stdout, so no ACAS run has ever pulled a file. It runs on
    # the same schedule as the rest — on demand, and once after a release that could change what
    # it exercises — and the peak note on sample-03 applies here too, since these jobs can all
    # be in flight at once for the packages below.
    #
    # Runs for the router (maf-sandbox), the backend (maf-sandbox-acas) and the kind supplying
    # the tool (maf-sandbox-codeact) — the same three as sample-03, because it is that stack with
    # both file channels wired.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-acas", "maf-sandbox-codeact"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Half of the federated-credential subject Azure checks, and where the non-secret sandbox
    # and model configuration lives.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # The same nine sample 03's job checks: the six this sample reads — no registry and no
      # image variable, because CODEACT_IMAGE is a hardcoded, fully-qualified MCR reference —
      # plus the three azure/login consumes below, whose own error for an empty client-id does
      # not say which field is missing. Both samples boot the same disk image, so nothing new
      # has to be imported.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            ACAS_SANDBOX_ENDPOINT ACAS_SANDBOX_SUBSCRIPTION_ID ACAS_SANDBOX_RESOURCE_GROUP \
            ACAS_SANDBOX_GROUP AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Run sample 14 and assert a file went in and a file came out
        env:
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          # A fresh checkout has no `out/`, but deleting it is what makes the on-disk half of
          # the assertion mean anything: a summary left by an earlier run would pass it without
          # this run having landed a thing.
          rm -rf samples/14_acas_codeact_files/out
          uv run --no-project samples/14_acas_codeact_files/agent.py | tee /tmp/sample14-out.txt
          # Sample 08's check, unchanged, over this backend's run — which is the claim the pair
          # is making. stdlib-only, so the runner's own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_codeact_files_sample.py \
            /tmp/sample14-out.txt samples/14_acas_codeact_files/out/summary.md

  sample-15:
    name: samples/15_acas_codeact_host_tools against a live sandbox
    # The only live dispatch in the set (#302), and the only place `Capability.HOST_TOOLS` is
    # exercised as traffic rather than as configuration. Sample 10 is its pair and needs nothing
    # at all — it decides everything at attach — so a red here while 10 is green is a statement
    # about the transport and the backend, never about the contract.
    #
    # It belongs on ACAS rather than Docker for a reason a local backend cannot supply: this
    # backend's `exec` is blocking and timeout-bounded, and the guest shim blocks on a response
    # file the host can only write while the program is still running. A dispatch that is
    # answered at all therefore proves the launcher detached and the supervisor took over, which
    # is exactly what declaring the capability claims here (#365).
    #
    # **The fourth billable job in this workflow**, and it creates two sandboxes rather than
    # one: a route each, because nothing deletes a run's transport files and a shared sandbox
    # would let the direct route's program read the dispatched route's responses off the guest
    # filesystem (#438). Both are deleted by the sample and checked by the assertion — a
    # dispatched program that hangs is #375, where nothing can dispose the sandbox holding it,
    # so the footer count is the thing that would notice.
    #
    # Runs for the router (maf-sandbox), the backend (maf-sandbox-acas) and the kind that owns
    # the dispatch wiring (maf-sandbox-codeact) — the same three as samples 03 and 14.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-acas", "maf-sandbox-codeact"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Half of the federated-credential subject Azure checks, and where the non-secret sandbox
    # and model configuration lives.
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Only when `HARNESS` points at it — see the `env` block above for why the checks come
      # from somewhere other than the samples. Sparse, because `scripts/` is all that is read.
      - name: Check out the checks from the default branch
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.repository.default_branch || 'main' }}
          path: .harness
          sparse-checkout: scripts

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # Sample 14's nine, unchanged: this sample boots the same disk image and reads the same
      # six variables, so a group already serving 14 needs nothing new imported.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            ACAS_SANDBOX_ENDPOINT ACAS_SANDBOX_SUBSCRIPTION_ID ACAS_SANDBOX_RESOURCE_GROUP \
            ACAS_SANDBOX_GROUP AZURE_OPENAI_ENDPOINT AZURE_OPENAI_CHAT_MODEL; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Run sample 15 and assert a program really called back out
        env:
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_CHAT_MODEL: ${{ vars.AZURE_OPENAI_CHAT_MODEL }}
        run: |
          set -euo pipefail
          uv run --no-project samples/15_acas_codeact_host_tools/agent.py | tee /tmp/sample15-out.txt
          # Both routes compute in the sandbox, so both are held to the totals their programs
          # printed; what the check compares is how many tool-calling rounds each road cost and
          # which of them carried the data. stdlib-only, so the runner's own python3 runs it.
          python3 "$HARNESS"/scripts/check_live_host_tools_dispatch_sample.py /tmp/sample15-out.txt

  acas-e2e:
    name: maf-sandbox-acas conformance against a live sandbox group
    # **The one job here that is not a sample**, and the one that does not install published
    # wheels: it runs the workspace suite from this checkout, because it tests a backend rather
    # than what a reader can install. Everything below it in this file answers "does the
    # published set still work"; this one answers "does the real service still do what this
    # backend believes", which no sample can (#306). A sample proves a happy path with a model
    # in the loop and covers no refusal at all — not the symlinked-parent walk, not the cap,
    # not teardown read back from the service instead of from process memory.
    #
    # It is also the only place four of the shared FILES_OUT probes meet the real service: they
    # require Capability.FILES_LIST and maf-sandbox-acas is the only backend declaring it, so
    # they skip in docker's suite, the one a pull request can run. A fake answers them on every
    # pull request in test_acas_backend.py — which is not the same claim, and #139 and #142 are
    # what the difference costs. The suite asserts nothing skipped rather than trusting a green
    # summary, since a skipped listing probe reports exactly like a passing one.
    #
    # **The fifth billable job**, and the cheapest of them per assertion: two sandboxes for
    # eighteen tests, no model, no tokens. It needs no model configuration and so reads neither
    # AZURE_OPENAI variable — the rule stated on the header applies, a job reads what it uses.
    #
    # maf-sandbox and maf-sandbox-acas only: the conformance probes live in the core and the
    # backend under test is the other, and no kind is involved.
    if: >-
      inputs.package == ''
      || contains(fromJSON('["maf-sandbox", "maf-sandbox-acas"]'), inputs.package)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: live-verify
    permissions:
      # Mints the OIDC token azure/login exchanges. No secret is involved.
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5

      # The five the suite reads plus the three azure/login consumes. No model variables: this
      # job calls no model. BICEP_SANDBOX_IMAGE is here because it supplies the guest below —
      # any Linux image in the registry with sh, ln, cat and mkfifo would do, and this is the
      # one already known to pull.
      - name: Check the live configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          ACAS_SANDBOX_REGISTRY: ${{ vars.ACAS_SANDBOX_REGISTRY }}
          BICEP_SANDBOX_IMAGE: ${{ vars.BICEP_SANDBOX_IMAGE }}
        run: |
          set -euo pipefail
          missing=()
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID \
            ACAS_SANDBOX_ENDPOINT ACAS_SANDBOX_SUBSCRIPTION_ID ACAS_SANDBOX_RESOURCE_GROUP \
            ACAS_SANDBOX_GROUP ACAS_SANDBOX_REGISTRY BICEP_SANDBOX_IMAGE; do
            [ -n "${!name}" ] || missing+=("$name")
          done
          if [ "${#missing[@]}" -ne 0 ]; then
            echo "::error::the live-verify environment is missing variables: ${missing[*]}. See docs/maintainers.md."
            exit 1
          fi

      - name: Sign in to Azure with OIDC
        uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2.3.1
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Sync workspace
        run: uv sync --locked

      # `-ra` because this suite skips itself when either variable it gates on is empty, and the
      # check above has already made that impossible: a skip here is a bug rather than a
      # configuration gap, and without the summary it reads identically to a pass.
      - name: Run the ACAS live suite
        env:
          ACAS_SANDBOX_ENDPOINT: ${{ vars.ACAS_SANDBOX_ENDPOINT }}
          ACAS_SANDBOX_SUBSCRIPTION_ID: ${{ vars.ACAS_SANDBOX_SUBSCRIPTION_ID }}
          ACAS_SANDBOX_RESOURCE_GROUP: ${{ vars.ACAS_SANDBOX_RESOURCE_GROUP }}
          ACAS_SANDBOX_GROUP: ${{ vars.ACAS_SANDBOX_GROUP }}
          ACAS_SANDBOX_REGISTRY: ${{ vars.ACAS_SANDBOX_REGISTRY }}
          MAF_SANDBOX_ACAS_E2E_IMAGE: ${{ vars.BICEP_SANDBOX_IMAGE }}
        run: |
          set -euo pipefail
          uv run pytest packages/maf-sandbox-acas/tests/test_acas_e2e.py -q -ra
