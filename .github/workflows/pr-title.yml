name: PR title

# This repository squash-merges, so the PR title becomes the commit subject release-please
# reads — the title is what decides the next version. Checked rather than requested.
on:
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: pr-title-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  lint:
    name: Conventional commit format
    runs-on: ubuntu-latest
    steps:
      # Pinned to the commit v5 resolves to, like every other action here.
      - uses: amannn/action-semantic-pull-request@e32d7e603df1aa1ba07e981f2a23455dee596825 # v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # `feat` and `fix` move versions; the rest are recorded and release nothing. A `!`
          # suffix or a BREAKING CHANGE footer bumps the minor, since every package is 0.x.
          types: |
            feat
            fix
            perf
            refactor
            docs
            test
            build
            ci
            chore
            revert
          # Scopes are free-form here: release-please attributes a change to a package by the
          # files it touches, not by the scope.
          requireScope: false
